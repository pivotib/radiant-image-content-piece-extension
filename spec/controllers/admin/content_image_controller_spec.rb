require File.dirname(__FILE__) + '/../../spec_helper'

describe Admin::ContentImageController do

  #Delete this example and add some real ones
  it "should use Admin::ContentImageController" do
    controller.should be_an_instance_of(Admin::ContentImageController)
  end

end
